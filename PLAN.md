# Our Space v2 — Подробный план

> Подарок для Аиды на день рождения (26 февраля 2026)
> Приватный сайт-история для двоих

---

## 1. Концепция

**"Горизонтальное путешествие"** — сайт ощущается как свиток вашей совместной истории. Скроллишь вниз — контент двигается горизонтально. Каждое событие — остановка на пути. Светлый, воздушный, с акцентами через типографику и SVG-анимации.

**Ключевые принципы:**
- Минимализм с душой (не пустой, а осмысленный)
- Горизонтальный таймлайн — центральный элемент
- SVG-анимации через Gemini для уникальности
- Максимально простой стек, минимум зависимостей
- Mobile-first (Аида скорее всего откроет с телефона)

---

## 2. Технический стек

| Компонент | Решение | Почему |
|-----------|---------|--------|
| **Фреймворк** | Next.js 15 (App Router) | SSR, удобный деплой на Vercel, API routes |
| **Язык** | TypeScript | Типобезопасность |
| **Стили** | Tailwind CSS v4 | Быстрая вёрстка, responsive |
| **Анимации** | GSAP + ScrollTrigger | Индустриальный стандарт для scroll-driven анимаций |
| **Smooth scroll** | Lenis | Плавный скролл, интеграция с GSAP |
| **SVG-анимации** | Gemini 2.5 → ручная интеграция | Уникальные, не шаблонные |
| **БД** | SQLite через Turso (libSQL) | Простая, бесплатная, быстрая |
| **ORM** | Drizzle ORM | Лёгкий, типобезопасный, идеален для SQLite |
| **Хранение фото** | Vercel Blob | Встроено в Vercel, бесплатный тир |
| **Аутентификация** | Два секретных ключа + cookie | Максимально просто |
| **Шрифты** | Google Fonts: DM Serif Display + DM Sans | Элегантный serif + чистый sans |
| **Иконки** | Lucide React | Минималистичные |
| **Деплой** | Vercel | Бесплатно, автодеплой из GitHub |

---

## 3. Аутентификация

Простейшая схема — никаких email/password/OAuth:

```
Пользователь 1 (Сабыржан): SECRET_KEY_1 = "сложная-случайная-строка-64-символа"
Пользователь 2 (Аида):      SECRET_KEY_2 = "другая-сложная-случайная-строка-64-символа"
```

**Как работает:**
1. Заходишь на сайт → видишь простое поле ввода "Введи свой ключ"
2. Вводишь ключ → POST /api/auth → сервер проверяет совпадение
3. Совпал → устанавливает HTTP-only cookie (JWT с userId: 1 или 2)
4. Cookie живёт 90 дней
5. Middleware проверяет cookie на каждом запросе

**Нет:**
- Нет регистрации
- Нет восстановления пароля
- Нет NextAuth
- Нет OAuth

---

## 4. База данных (схема)

Используем Drizzle ORM + Turso (SQLite):

```sql
-- Пользователи (захардкожены при seed, не создаются через UI)
users:
  id          INTEGER PRIMARY KEY  -- 1 = Сабыржан, 2 = Аида
  name        TEXT NOT NULL
  avatar_url  TEXT                 -- URL аватарки в Vercel Blob

-- События таймлайна
events:
  id          INTEGER PRIMARY KEY AUTOINCREMENT
  title       TEXT NOT NULL        -- "Первое свидание"
  description TEXT                 -- Подробное описание
  date        TEXT NOT NULL        -- "2024-09-02" (ISO date)
  photo_url   TEXT                 -- URL фото в Vercel Blob
  created_by  INTEGER REFERENCES users(id)
  created_at  TEXT DEFAULT (datetime('now'))
  sort_order  INTEGER DEFAULT 0   -- Для ручной сортировки

-- "Первый раз когда..." 
firsts:
  id          INTEGER PRIMARY KEY AUTOINCREMENT
  title       TEXT NOT NULL        -- "мы поссорились"
  description TEXT                 -- "и помирились через 20 минут"
  date        TEXT                 -- Опционально, "2023-10-15"
  sort_order  INTEGER DEFAULT 0
  created_at  TEXT DEFAULT (datetime('now'))

-- Записки (послания)
notes:
  id          INTEGER PRIMARY KEY AUTOINCREMENT
  content     TEXT NOT NULL        -- Текст записки
  author_id   INTEGER REFERENCES users(id)
  is_read     INTEGER DEFAULT 0   -- 0 = не прочитана
  created_at  TEXT DEFAULT (datetime('now'))

-- Капсулы времени (записки с датой вскрытия)
capsules:
  id          INTEGER PRIMARY KEY AUTOINCREMENT
  content     TEXT NOT NULL
  author_id   INTEGER REFERENCES users(id)
  unlock_at   TEXT NOT NULL        -- Дата когда можно прочитать
  is_read     INTEGER DEFAULT 0
  created_at  TEXT DEFAULT (datetime('now'))
```

---

## 5. Структура страниц / Роуты

```
/                     → Главная (redirect на /timeline если авторизован)
/login                → Страница ввода ключа
/timeline             → ОСНОВНАЯ СТРАНИЦА: горизонтальный таймлайн
/firsts               → "Первый раз когда..." (бесконечная лента)
/notes                → Записки друг другу
/capsules             → Капсулы времени (опционально)

/api/auth             → POST: проверка ключа, установка cookie
/api/auth/logout      → POST: удаление cookie
/api/events           → CRUD для событий таймлайна
/api/firsts           → CRUD для "первых раз"
/api/notes            → CRUD для записок
/api/capsules         → CRUD для капсул
/api/upload           → POST: загрузка фото в Vercel Blob
```

---

## 6. Layout и навигация

### Desktop (≥768px)

```
┌──────────────────────────────────────────────────────────────┐
│  TOP BAR (fixed, blur backdrop)                              │
│                                                              │
│  Our Space        [Таймлайн] [Первый раз] [Записки]    547  │
│                                                         дней │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│                    ОСНОВНОЙ КОНТЕНТ                          │
│              (зависит от текущей страницы)                    │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### Mobile (<768px)

```
┌─────────────────────┐
│  Our Space  547 дней│  ← Top bar (compact)
├─────────────────────┤
│                     │
│   ОСНОВНОЙ КОНТЕНТ  │
│                     │
├─────────────────────┤
│ [🕐] [✨] [💌] [⏳]│  ← Bottom nav (fixed)
│ Лента Первый Записки│     Капсулы
└─────────────────────┘
```

---

## 7. Дизайн по страницам

### 7.1 Страница входа (/login)

Минималистичная. Фон — тёплый off-white. По центру:
- Название "Our Space" (DM Serif Display, крупно)
- Одно поле ввода — "Введи свой ключ"
- Кнопка "Войти"
- Ничего лишнего. Чисто, элегантно.

### 7.2 Таймлайн (/timeline) — ГЛАВНАЯ

**Горизонтальный скролл** (вертикальный scroll мыши → горизонтальное движение):

```
◄════════════════════════════════════════════════════════════►

  [СТАРТ]     [Событие 1]     [Событие 2]     [Событие 3]
              
  Дата начала   25.08.2024      02.09.2024      15.10.2024
  Тонкая        Как мы          Первое          Первая 
  золотая       познакомились   свидание        поездка
  нить ─────●───────────────●───────────────●────────────►
  соединяет     
  все события   [ФОТО]          [ФОТО]          [ФОТО]
                + описание      + описание      + описание
```

**Детали:**
- Каждое событие — карточка с фото, датой, заголовком, описанием
- Фото разных размеров (не одинаковые!)
- SVG-нить/линия соединяет все события (анимируется при скролле — "рисуется")
- Карточки появляются с fade-in + slight translateY при приближении
- Между карточками — декоративные SVG-элементы (Gemini)
- Кнопка "+" для добавления нового события (плавающая)

**Мобильная версия:**
- Вертикальный скролл (горизонтальный неудобен на мобиле)
- Карточки друг под другом
- Нить слева вертикально

### 7.3 "Первый раз когда..." (/firsts)

**Бесконечная лента** (вариант Б из обсуждения):

```
                                                    
Первый раз когда мы поссорились           15 октября 2024
                                           и помирились через 20 минут
                                                    
─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ 
                                                    
Первый раз когда ты заснула рядом          3 ноября 2024
                                           в автобусе, на моём плече
                                                    
─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ 
                                                    
Первый раз когда я понял                   28 декабря 2024
                                           что не могу без тебя
```

**Детали:**
- Каждый пункт появляется при скролле (fade-in, staggered)
- Слева — "Первый раз когда [текст]" (крупный DM Serif Display)
- Справа — дата (мелко, muted) + описание
- Между пунктами — тонкий разделитель или много пустого пространства
- Опционально: маленькие SVG line-art иллюстрации (Gemini) рядом с каждым
- Кнопка "+" для добавления

### 7.4 Записки (/notes)

Простой, чистый интерфейс:
- Список записок (новые сверху)
- Каждая записка — карточка: текст, от кого, дата
- Непрочитанные — subtle accent (тонкая золотая линия слева)
- Кнопка "Написать записку" → модальное окно с textarea
- При открытии → auto-mark as read

### 7.5 Капсулы времени (/capsules) — если успеем

- Список капсул
- Запечатанные: видно "От Сабыржана · откроется через 47 дней" + SVG-конверт
- Открытые: как записка
- Кнопка создания: текст + дата вскрытия

---

## 8. SVG-анимации (Gemini)

### Что генерируем через Gemini:

| Элемент | Описание | Приоритет |
|---------|----------|-----------|
| **Счётчик дней** | Цифры собираются из линий/частиц | 🔴 Высокий |
| **Нить таймлайна** | Органическая линия между событиями | 🔴 Высокий |
| **Декор между событиями** | Маленькие абстрактные элементы (звёзды, волны, листья) | 🟡 Средний |
| **Иллюстрации "Первый раз"** | Line-art для каждого пункта (чашка, луна, сердце) | 🟡 Средний |
| **Фон** | Тонкие движущиеся волны/линии на фоне | 🟢 Низкий |

### Workflow:
1. Я пишу детальный промпт для Gemini
2. Ты прогоняешь → получаешь SVG
3. Скидываешь мне → я интегрирую в проект
4. Итеративно правим если надо

---

## 9. Цветовая палитра

```css
/* Фоны */
--bg-primary:    #f8f6f3;    /* Тёплый off-white */
--bg-secondary:  #f0ece7;    /* Чуть темнее для карточек */
--bg-elevated:   #ffffff;    /* Белый для приподнятых элементов */

/* Текст */
--text-primary:  #1a1a1a;    /* Почти чёрный */
--text-secondary:#6b6560;    /* Тёплый серый */
--text-muted:    #a09890;    /* Приглушённый */

/* Акценты */
--accent-gold:   #b8960c;    /* Тёплое золото (нить, акценты) */
--accent-warm:   #c4956a;    /* Тёплый для hover */

/* Границы */
--border:        #e8e2db;    /* Тёплая граница */
```

---

## 10. Типографика

```css
/* Display — заголовки, даты, "Первый раз когда..." */
font-family: 'DM Serif Display', Georgia, serif;

/* Body — основной текст, UI, описания */
font-family: 'DM Sans', system-ui, sans-serif;
```

**Шкала:**
- Hero/Счётчик: 4rem+ (64px)
- Заголовок страницы: 2.5rem (40px)
- Заголовок события: 1.5rem (24px)
- Описание: 1rem (16px)
- Мелкий текст/даты: 0.875rem (14px)

---

## 11. Структура файлов

```
our-space/
├── src/
│   ├── app/
│   │   ├── layout.tsx              # Root layout + шрифты
│   │   ├── page.tsx                # Redirect → /timeline
│   │   ├── globals.css             # Tailwind + CSS переменные
│   │   ├── login/
│   │   │   └── page.tsx            # Страница входа
│   │   ├── timeline/
│   │   │   └── page.tsx            # Горизонтальный таймлайн
│   │   ├── firsts/
│   │   │   └── page.tsx            # "Первый раз когда..."
│   │   ├── notes/
│   │   │   └── page.tsx            # Записки
│   │   ├── capsules/
│   │   │   └── page.tsx            # Капсулы времени
│   │   └── api/
│   │       ├── auth/
│   │       │   └── route.ts        # Auth endpoints
│   │       ├── events/
│   │       │   └── route.ts        # CRUD события
│   │       ├── firsts/
│   │       │   └── route.ts        # CRUD "первый раз"
│   │       ├── notes/
│   │       │   └── route.ts        # CRUD записки
│   │       ├── capsules/
│   │       │   └── route.ts        # CRUD капсулы
│   │       └── upload/
│   │           └── route.ts        # Загрузка фото
│   ├── components/
│   │   ├── layout/
│   │   │   ├── TopBar.tsx          # Верхняя панель
│   │   │   ├── BottomNav.tsx       # Мобильная навигация
│   │   │   └── DayCounter.tsx      # SVG счётчик дней
│   │   ├── timeline/
│   │   │   ├── HorizontalTimeline.tsx  # Основной компонент
│   │   │   ├── TimelineCard.tsx    # Карточка события
│   │   │   ├── TimelineThread.tsx  # SVG нить
│   │   │   └── AddEventModal.tsx   # Модал добавления
│   │   ├── firsts/
│   │   │   ├── FirstsList.tsx      # Список "первых раз"
│   │   │   └── AddFirstModal.tsx   # Модал добавления
│   │   ├── notes/
│   │   │   ├── NotesList.tsx       # Список записок
│   │   │   ├── NoteCard.tsx        # Карточка записки
│   │   │   └── NoteComposer.tsx    # Модал написания
│   │   └── ui/
│   │       ├── Button.tsx
│   │       ├── Input.tsx
│   │       ├── Modal.tsx
│   │       └── Card.tsx
│   ├── lib/
│   │   ├── db.ts                   # Drizzle + Turso клиент
│   │   ├── schema.ts              # Drizzle схема (таблицы)
│   │   ├── auth.ts                # Проверка ключей, JWT
│   │   └── utils.ts               # Хелперы (даты, плюрализация)
│   ├── actions/
│   │   ├── events.ts              # Server actions для событий
│   │   ├── firsts.ts              # Server actions для "первых раз"
│   │   ├── notes.ts               # Server actions для записок
│   │   └── capsules.ts            # Server actions для капсул
│   └── middleware.ts              # Auth middleware
├── public/
│   └── svg/                       # SVG-анимации (от Gemini)
│       ├── thread.svg
│       ├── counter-particles.svg
│       └── decorations/
├── drizzle/
│   └── migrations/                # SQL миграции
├── .env.example
├── .env.local                     # (не в git)
├── package.json
├── tailwind.config.ts
├── tsconfig.json
├── drizzle.config.ts
├── next.config.ts
└── PLAN.md                        # Этот файл
```

---

## 12. Переменные окружения

```env
# Auth — два секретных ключа
SECRET_KEY_1=random-64-char-string-for-sabyrzhan
SECRET_KEY_2=random-64-char-string-for-aida

# JWT
JWT_SECRET=another-random-string

# Database (Turso)
TURSO_DATABASE_URL=libsql://your-db.turso.io
TURSO_AUTH_TOKEN=your-token

# File storage (Vercel Blob)
BLOB_READ_WRITE_TOKEN=vercel-blob-token

# App
NEXT_PUBLIC_APP_URL=https://our-space.vercel.app
RELATIONSHIP_START_DATE=2024-08-25

# User names (hardcoded)
USER_1_NAME=Сабыржан
USER_2_NAME=Аида
```

---

## 13. Timeline (этапы работы)

### Этап 1: Фундамент (20-21 фев)
- [ ] Инициализация Next.js проекта
- [ ] Tailwind CSS v4 + дизайн-система (цвета, шрифты)
- [ ] Drizzle ORM + Turso setup + схема БД
- [ ] Auth (два ключа, cookie, middleware)
- [ ] Layout (TopBar + BottomNav)
- [ ] Деплой на Vercel (пустой сайт, но уже онлайн)

### Этап 2: Ядро (22-23 фев)
- [ ] Горизонтальный таймлайн (GSAP + ScrollTrigger + Lenis)
- [ ] Карточки событий + загрузка фото
- [ ] "Первый раз когда..." — бесконечная лента
- [ ] Записки (создание + чтение + непрочитанные)
- [ ] Счётчик дней

### Этап 3: Полировка + SVG (24-25 фев)
- [ ] SVG-анимации (Gemini промпты → интеграция)
- [ ] Переходы, микроанимации
- [ ] Мобильная адаптация (тестирование)
- [ ] Наполнение контентом (реальные фото и события)
- [ ] Капсулы времени (если успеваем)

### 26 фев: Готово 🎁
- [ ] Финальная проверка
- [ ] Отправить Аиде ключ

---

## 14. Принципы разработки

1. **Mobile-first** — Аида скорее всего откроет с телефона
2. **Progressive enhancement** — сначала работает без JS, потом добавляем анимации
3. **Нет оверинжиниринга** — простые решения > сложные
4. **Контент > фичи** — лучше 3 фичи с реальным контентом, чем 10 пустых
5. **Итеративно** — деплоим рано, правим часто
