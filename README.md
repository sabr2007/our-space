# Our Space v2

> Подарок для Аиды на день рождения (26 февраля 2026)  
> Приватный сайт-история для двоих

**Дизайн:** Noir Rose — тёмный романтичный film noir с приглушёнными розовыми акцентами

---

## ✅ Что уже сделано

- ✅ Next.js 15 + TypeScript + Tailwind v4
- ✅ Noir Rose дизайн-система (тёмная тема)
- ✅ Drizzle ORM + схема БД (5 таблиц)
- ✅ Auth система (2 секретных ключа + JWT)
- ✅ Middleware для защиты роутов
- ✅ Страницы: Login, Timeline, Notes, Firsts
- ✅ UI компоненты (Button, Input)
- ✅ Утилиты (даты, плюрализация на русском)
- ✅ Промпты для Gemini SVG
- ✅ Проект собирается (`npm run build` успешный)

---

## 🛠️ Что нужно сделать тебе

### 1. Настроить Turso (БД)

**Turso** — это SQLite в облаке, бесплатный тир.

```bash
# Установить Turso CLI
curl -sSfL https://get.tur.so/install.sh | bash

# Войти (создаст аккаунт если нет)
turso auth login

# Создать БД
turso db create our-space

# Получить URL и токен
turso db show our-space --url
turso db tokens create our-space
```

Добавить в `.env.local`:
```env
TURSO_DATABASE_URL=libsql://твой-url.turso.io
TURSO_AUTH_TOKEN=твой-токен
```

Сгенерировать миграции и применить:
```bash
npm run db:generate
npm run db:push
```

---

### 2. Настроить Vercel

1. Пуш на GitHub (уже сделано)
2. Зайди на [vercel.com](https://vercel.com)
3. Import GitHub repo `sabr2007/our-space`
4. Добавь env переменные:
   - `SECRET_KEY_1` — сгенерируй случайную строку 64+ символов
   - `SECRET_KEY_2` — ещё одну случайную строку
   - `JWT_SECRET` — ещё одну
   - `TURSO_DATABASE_URL` — из шага 1
   - `TURSO_AUTH_TOKEN` — из шага 1
   - `RELATIONSHIP_START_DATE` — `2024-08-25` (или твоя дата)
   - `USER_1_NAME` — `Сабыржан`
   - `USER_2_NAME` — `Аида`
5. Deploy!

**Генерация ключей:**
```bash
# В консоли браузера или Node REPL
Array(64).fill(0).map(() => Math.random().toString(36)[2]).join('')
```

---

### 3. Сгенерировать SVG через Gemini

Открой файл `GEMINI-PROMPTS.md` — там 5 промптов для Gemini 3.1:

1. **Счётчик дней** — число собирается из частиц
2. **Timeline thread** — органическая линия между событиями
3. **Декоративные элементы** — 5 маленьких иконок между событиями
4. **Line art icons** — 6 иконок для "Первый раз когда..."
5. **Background ambient** — тонкие движущиеся линии (опционально)

Для каждого:
1. Копируй промпт в Gemini 3.1
2. Получи SVG
3. Сохрани в `public/svg/` (создай папку)
4. Скинь мне — я интегрирую в компоненты

---

### 4. Vercel Blob (хранение фото)

После деплоя на Vercel:

1. В dashboard Vercel → твой проект → Storage
2. Create Storage → Blob
3. Скопируй `BLOB_READ_WRITE_TOKEN`
4. Добавь в env переменные

---

## 🚀 Локальная разработка

```bash
# Установить зависимости (уже сделано)
npm install

# Запустить dev server
npm run dev

# Открыть http://localhost:3000
```

**Тестовые ключи** (в `.env.local`):
- `test-key-sabyrzhan-please-change-later-xxxxxxxxxxxxxxxxxxxxxx`
- `test-key-aida-please-change-later-yyyyyyyyyyyyyyyyyyyyyyyy`

Не забудь поменять на реальные перед деплоем!

---

## 📂 Структура проекта

```
app/
├── api/auth/login/        # API для логина
├── login/                 # Страница входа
├── timeline/              # Горизонтальный таймлайн
├── notes/                 # Записки друг другу
├── firsts/                # "Первый раз когда..."
├── layout.tsx             # Root layout
└── globals.css            # Noir Rose дизайн-система

lib/
├── auth.ts                # JWT, проверка ключей
├── db.ts                  # Drizzle + Turso подключение
├── schema.ts              # БД схема (5 таблиц)
└── utils.ts               # Даты, плюрализация

components/ui/
├── Button.tsx
└── Input.tsx

middleware.ts              # Защита роутов
drizzle.config.ts          # Drizzle CLI config
```

---

## 🎨 Дизайн-система "Noir Rose"

**Цвета:**
- Фон: `#0f0a0d` (очень тёмный тёплый)
- Поверхности: `#1a1215`
- Текст: `#f5e8ec` (светлый розоватый)
- Акцент: `#e85d75` (приглушённый розовый)
- Ховер: `#ff6b88` (тёплый розовый)

**Шрифты:**
- Display: Bodoni Moda (serif, luxury)
- Body: Lato (sans, чистый)

**Эффекты:**
- `.glow-rose` — розовое свечение
- `.transition-smooth` — плавные переходы

---

## 📋 TODO (что я доделаю дальше)

- [ ] Горизонтальный скролл таймлайна (GSAP + ScrollTrigger)
- [ ] Модальные окна (добавление событий, записок, firsts)
- [ ] Server Actions для CRUD
- [ ] Интеграция SVG анимаций
- [ ] TopBar компонент (навигация)
- [ ] Mobile navigation
- [ ] Счётчик дней с SVG анимацией
- [ ] Капсулы времени (если успеем)
- [ ] Загрузка фото через Vercel Blob
- [ ] Оптимизация и полировка

---

## 🐛 Известные issues

- Middleware deprecated warning — Next 16 рекомендует proxy, но middleware всё ещё работает
- Multiple lockfiles warning — это норма, можно игнорировать

---

## 📞 Контакты

Если что-то не работает — пиши мне в Telegram, разберёмся! 🦀
